@model IEnumerable<TicketPlace2._0.Models.EvenementTypePlaceModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Evenement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypePlace)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreDePlaces)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OnCreate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OnUpdate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Evenement.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypePlace.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreDePlaces)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OnCreate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OnUpdate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
